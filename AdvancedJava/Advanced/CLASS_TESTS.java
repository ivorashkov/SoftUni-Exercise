package AdvancedJava.Advanced;

public class CLASS_TESTS {

    public static void main(String[] args) {

//        StringBuilder sb = new StringBuilder();
//        sb.append(String.format("%s basket contains:", material));
//        data.forEach(egg -> sb.append(System.lineSeparator()).append(egg.toString()));
//        return sb.toString().trim();


//   public boolean removeEgg(String color) {
//       return this.data.removeIf(s -> s.getColor().equals(color));
//        Egg egg = this.getEgg(color);
//        data.remove(egg);
//        return  egg != null;*//*
//        }

//        public Egg getStrongestEgg() {
//            return data.stream().max(Comparator.comparing(Egg::getStrength)).orElse(null);
//        }

//        public Egg getEgg(String color) {
//            return data.stream().filter(e -> e.getColor().equals(color))
//                    .findFirst().orElse(null);
//        }

//        public String report() {
//            StringBuilder sb = new StringBuilder();
//            sb.append(String.format("Parrots available at %s:", this.getName()));
//            data.stream().filter(p -> p.isAvailable()).forEach(p -> sb.append(System.lineSeparator()).append(p.toString()));
//            return sb.toString();
//        }

//        public Parrot sellParrot(String name) {
//            Parrot parrot = this.data.stream().filter(p -> p.getName().equals(name))
//                    .findAny().orElse(null);
//
//            if (parrot != null) {
//                parrot.setAvailable(false);
//            }
//            return parrot;
//        }
//
//        public List<Parrot> sellParrotBySpecies(String species) {
//            List<Parrot> parrots = new ArrayList<>();
//            this.data.stream().filter(p->p.getSpecies().equals(species))
//                    .forEach(p->{
//                        parrots.add(p);
//                        p.setAvailable(false);
//                    });
//            return parrots;

//        public String getStatistics(){
//            StringBuilder str = new StringBuilder();
//            str.append("The people in the hotel " + getName() + " are:");
//            getRoster().forEach(p ->str.append(String.format("%n%s", p.toString())));
//            return str.toString();

//        public AdvancedJava.OOP.Encapsulation.SortByNameAndAge.AdvancedJava.OOP.Encapsulation.SalaryIncrease.AdvancedJava.OOP.Encapsulation.ValidationData.AdvancedJava.OOP.Encapsulation.FirstAndReserveTeam.AdvancedJava.OOP.Inheritance.Person getPerson(String name,String hometown){
//            return this.roster.stream().filter(person -> person.getName().equals(name)
//                    && person.getHometown().equals(hometown)).findAny().orElse(null);
//    }

//        public boolean remove(String name){
//            for (AdvancedJava.OOP.Encapsulation.SortByNameAndAge.AdvancedJava.OOP.Encapsulation.SalaryIncrease.AdvancedJava.OOP.Encapsulation.ValidationData.AdvancedJava.OOP.Encapsulation.FirstAndReserveTeam.AdvancedJava.OOP.Inheritance.Person p: this.roster){
//                if (p.getName().equals(name)){
//                    return roster.remove(p);
//                }
//            }
//            return false;
//        }
    }
}

